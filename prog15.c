//program wrong unfinished//
#include<stdio.h>
void main()
{
	int a,n[10],num;
	for(a=1;a<=10;a++)
	{
		scanf("%d",&n[a]);
	}
	printf("enter the number you need to find :");
	scanf("%d",&num);
	if(n[a]==num)
	{
		printf("number is found");
	}
	else
	{
		printf("number not found");
	}
}
